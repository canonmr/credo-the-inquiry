---
export interface Props {
  established: string[];
  contested: string[];
  not_established: string[];
  lang: 'id' | 'en';
}

const p = Astro.props as Props;
const isId = p.lang === 'id';
---

<aside class="ledger" aria-labelledby="ledger-heading">
  <h2 id="ledger-heading">
    {isId ? 'Ringkasan bab ini' : 'Epistemic ledger'}
  </h2>
  <p
    style="font-family: var(--font-sans); font-size: var(--fs-small); color: var(--color-ink-soft); margin: 0 0 var(--gap-2) 0;"
  >
    {
      isId
        ? 'Status epistemik bab ini: apa yang sudah mapan, apa yang masih diperdebatkan, dan apa yang belum dapat disimpulkan.'
        : 'Epistemic status of this chapter: what is established, contested, and not established.'
    }
  </p>
  <div class="columns">
    <div class="column established">
      <div class="label">{isId ? 'Sudah jelas' : 'Established'}</div>
      <ul>
        {p.established.map((x) => <li>{x}</li>)}
      </ul>
    </div>
    <div class="column contested">
      <div class="label">{isId ? 'Masih diperdebatkan' : 'Contested'}</div>
      <ul>
        {p.contested.map((x) => <li>{x}</li>)}
      </ul>
    </div>
    <div class="column not-est">
      <div class="label">{isId ? 'Belum bisa dipastikan' : 'Not established'}</div>
      <ul>
        {p.not_established.map((x) => <li>{x}</li>)}
      </ul>
    </div>
  </div>
</aside>
